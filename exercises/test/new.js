var myString = "The big brown horse likes to run";

// take each individual word and make it a var
// lengthOf that var
// compare that word's length aginst each other word in the string, with for loop
// if its bigger current for loop index return that word
function findLongest(myString) {
    
    var word = [];
    
    var empty = [];
    
    for (var i = 0; i < myString.length; i++) {
        if (word.length > [i].length) {
            
        }
    }
}